package main

import (
    "f
mt"    
"sync" 
 

  "time")func
 
main() {        var wg
 sync.Wait
Group    wg.
Add(1)    go func
() {        defer wg.Done()
        for i := 1; i <= 
5; i++ {            fmt.Printl
n("
num:
", i)     
       time.Sleep(1 * time.Second)        }    }(
)
    wg.Wait()    fmt.Println("Горутина завершилась, продолжаем.")}
